services:
  - id: redis
    container: redis
    ports:
      - 6379
    environment:
      TEST_ENV: "TEST_ENV_VALUE"
  - id: pulsar
    container: apachepulsar/pulsar
    cmd: bin/pulsar standalone
    dependencies:
      - redis #Not an actual dependency, used just for testing

# Wait strategy?